Drowsiness Detection: Developed a real-time drowsiness detection system using OpenCV, dlib, and facial landmark detection, capable of identifying eye blinks and alerting the user based on their eye state.

-----------process---------------- 1.just extract all these files and save it in a folder 2.Latrer,just open IDLE and open the ".py " file 3. import all the modules in your system by executing commands in cmd prompt 4.now after executing all the commands, just run the code 5.Make sure that all the files are in the same folder so that the landmarks in the folder are correctly identified
